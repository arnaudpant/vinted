import logo from '@/assets/logoVinted.svg';

const VintedLogo: () => JSX.Element = () => {
  return <img src={logo} alt="logo Vinted" className="ml-4 cursor-pointer" />;
};

export default VintedLogo;
